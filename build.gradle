apply plugin: 'java'
apply plugin: 'idea'

repositories {
  mavenCentral()
}

dependencies {
    compile([group: 'com.google.guava', name: 'guava', version: 'r07'])

	testCompile([group: 'junit', name: 'junit', version: '4.8.2'])
}

ideaProject.javaVersion = '1.6'
